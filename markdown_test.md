# Markdown 测试文档

这是一篇用于测试 Rich Markdown 渲染效果的文档，包含了各种常见的 Markdown 语法元素。

## 一、文本格式

这是一段普通文本。以下是一些格式化的文本：

- **粗体文本** 使用双星号
- *斜体文本* 使用单星号
- ***粗斜体文本*** 使用三星号
- ~~删除线文本~~ 使用双波浪线
- `行内代码` 使用反引号

## 二、列表

### 无序列表

- 第一项
- 第二项
  - 嵌套项 A
  - 嵌套项 B
- 第三项

### 有序列表

1. 第一步：安装依赖
2. 第二步：配置环境
3. 第三步：运行程序

### 任务列表

- [x] 已完成任务
- [ ] 未完成任务
- [ ] 待办事项

## 三、代码块

### Python 示例

```python
def fibonacci(n):
    """计算斐波那契数列"""
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

# 打印前10个斐波那契数
for i in range(10):
    print(fibonacci(i))
```

### JavaScript 示例

```javascript
const greeting = (name) => {
    return `Hello, ${name}!`;
};

console.log(greeting('World'));
```

### Shell 示例

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

## 四、引用

> 这是一段引用文本。
> 
> 引用可以包含多个段落。
>
> — 作者名

## 五、表格

| 功能 | 状态 | 描述 |
|------|------|------|
| 语法高亮 | ✅ | 支持多种编程语言 |
| 代码块 | ✅ | 带语言标识 |
| 表格 | ✅ | 支持 GFM 表格 |
| 任务列表 | ⚠️ | 部分支持 |

## 六、链接和图片

这是一个 [链接示例](https://github.com)。

图片示例（如果支持）：

![示例图片](https://via.placeholder.com/150)

## 七、分隔线

下面是一条分隔线：

---

上面是分隔线。

## 八、数学公式（如果支持）

行内公式：$E = mc^2$

块级公式：

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

## 九、脚注

这是一个脚注示例[^1]。

[^1]: 这是脚注的内容。

## 十、总结

这篇文档包含了：

1. ✅ 标题（一级到六级）
2. ✅ 文本格式（粗体、斜体、删除线、代码）
3. ✅ 列表（有序、无序、任务）
4. ✅ 代码块（带语法高亮）
5. ✅ 引用
6. ✅ 表格
7. ✅ 链接和图片
8. ✅ 分隔线
9. ⚠️ 数学公式（可能不支持）
10. ⚠️ 脚注（可能不支持）

---

**提示**：Rich 的 Markdown 支持可能因版本而异，以上元素的实际渲染效果请以显示结果为准。